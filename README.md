# IOTdashboard
a generic dashboard for multiple IOT projects.

## Project status:
 currently this is just draggable bootstrap panels.

## Todo's:
  - have the gridstack element always full height
  - editable titles
  - serializable panels/layout
  - de-serializing panels/layout
  - panel content types:
    - single line graph
    - video feed
    - multi-line graph
    - bar graph
  - accounts
  - rest API with API keys
  - stored dashboards in DB
  - stored data in DB
